{
  "name": "@wudly/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Wudly Signal — Browser-Extension (Manifest V3): das ehrliche Besitzer-Urteil direkt im Shop.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@wudly/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
